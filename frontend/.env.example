# Where Next.js proxies /api/* (server-side, build time). Backend base URL, no trailing slash.
BACKEND_URL=http://localhost:3001
# Where the browser opens the game WebSocket. Use wss:// in production.
NEXT_PUBLIC_WS_URL=ws://localhost:3001/ws
